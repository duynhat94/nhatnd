import 'package:flutter/material.dart';

class BaseCubitExampleScreen extends StatelessWidget {
  const BaseCubitExampleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('base cubit example')));
  }
}
